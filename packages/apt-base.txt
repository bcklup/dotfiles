# apt-base.txt — essential CLI packages for Ubuntu / Debian / WSL.
# One package per line; blank lines and # comments ignored.
# bootstrap.sh installs these with `sudo apt-get install -y`.
#
# NOTE: a few tools aren't in default apt repos on older releases and are
# handled separately in bootstrap.sh (see install_extras_apt): eza, git-delta,
# lazygit, zoxide, tlrc. Everything reliably in apt lives here.

zsh
git
curl
wget
build-essential
neovim
tmux
ripgrep
fd-find
bat
fzf
jq
ncdu
unzip
ca-certificates
python3
python3-pip
python3-venv
golang-go
